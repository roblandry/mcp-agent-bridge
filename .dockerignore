# VCS
.git
.gitignore
.gitattributes

# Python build/runtime artifacts
.venv
__pycache__
*.pyc
*.pyo
*.pyd
*.egg-info
.pytest_cache
.mypy_cache
.ruff_cache
.tox

# Local bridge runtime state (must NEVER be baked into image)
bridge.db
*.db
peers.json
payloads/

# Editor / OS junk
.idea
.vscode
.DS_Store
*.swp
*.swo

# CI / docs we don't need in the build context
.github
README.md
LICENSE
CLAUDE.md

# Local test driver — server.py is the only runtime artifact
smoke.py
